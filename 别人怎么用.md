# 别人怎么用这个池子

> 写给"想参与的人"。**先读 [`CONTRIBUTING.md`](CONTRIBUTING.md)（五分钟），
> 遇到细节问题再读 [`CONTRIBUTORS.md`](CONTRIBUTORS.md)（完整手册）。**
> 这份文件回答的是**具体动作**：他打开浏览器之后点什么、敲什么命令。

---

## 一、他现在能在哪里看到这个池子

| 入口 | 地址 | 能做什么 |
|---|---|---|
| **协作界面** | `http://<你的域名或 IP>:3000/JiaWeathour/pool` | 看公告板、开 issue、提 PR |
| **公开展示页** | Cloudflare Pages 的网址 | 只看，不用登录；手机也好看 |
| **直接 clone** | `git clone <仓库地址>` | 拿走全部数据 |

**★ 最重要的一个入口是「公告板」。** 它有三个地方，内容一样：

1. `data/requests.jsonl` —— 原始数据
2. 协作界面的 Issues 列表
3. 展示页的第一屏

---

## 二、新人第一次：三步，五分钟

### 第 1 步：拿到仓库

```bash
git clone http://<域名>:3000/JiaWeathour/pool.git pool
cd pool
```

### 第 2 步：生成自己的身份

```bash
python3 tools/sign init --handle 你的名字
```

输出：

```
✓ 私钥：/home/你/.pool/keys/你的名字.key（权限 600）
  公钥：ed25519:xxxxx
  已登记到 data/keyring.jsonl，并记住 handle = 你的名字
```

**★ 然后立刻备份那个私钥文件。** 拷到加密 U 盘或密码管理器，**不要放进仓库**。

**这一步是他和这个池子唯一的绑定。** 私钥丢了，他过去的签名仍然有效（因为当时确实是他签的），但**再也无法**用那个身份签新东西。

### 第 3 步：开一张单

```bash
python3 tools/sign req \
  --kind knowledge_pack \
  --title "一句话说清缺什么" \
  --spec "产物长什么样：形态、范围、精度要求" \
  --acceptance "怎么算做完了（必须可判定）"
```

**或者**在网页上点「新建 Issue」，选「开一张单」表单，填同样几栏。

**★ 但这两件事不是一回事，必须说清：**

| | 是什么 | 进账本吗 | 可验证吗 |
|---|---|---|---|
| **网页 issue** | 公告板上的一个帖子 | ❌ | ❌ |
| **`tools/sign req` 开的单** | **记录**（有签名、链进账本） | ✅ | ✅ |

**网页 issue 只是"让人先看到"，它不可验证，也不算记录。**
**正式开单必须用 `tools/sign req`。**

**想两边都有，就都做一次**，然后：

```bash
python3 tools/sign req --kind ... --link <issue 编号>
```

**开单不需要任何人批准。** 它是他的一句声明，CI 过了就进主干。

---

## 三、他要做的四件事，逐个说

### ① 提需求（开单）

- **命令行**：`tools/sign req ...`
- **网页**：新建 Issue → 选模板
- **唯一不能省的**：`--acceptance`（怎么算做完了）

**验收标准写不好的典型症状：**

| 写法 | 问题 |
|---|---|
| "写得好一点" | 没法判定 |
| "尽量全面" | 没法判定 |
| "包含至少 3 条可核查的锚点（格式 `path:行号`），且写明什么证据会推翻它" | ✅ 可以判定 |

**判断方法：把这句话给一个不认识他的人，他能不能只靠它决定"这东西算不算做完了"。**

### ② 交东西（交付）

```bash
# 先接单
python3 tools/sign claim r-xxxxxx-0001

# 准备产物内容
cat > 我的产物.json <<'JSON'
{
  "claim": "一句话结论",
  "anchors": ["路径:行号", "路径:行号"],
  "counterevidence": "什么证据会推翻它",
  "stopping_condition": "推理到哪里必须停"
}
JSON

# 交付
python3 tools/sign deliver \
  --request r-xxxxxx-0001 \
  --kind knowledge_pack \
  --content-file 我的产物.json
```

`--kind` 决定 `content` 要填哪几栏：

| kind | 必填 |
|---|---|
| `knowledge_pack` | claim / anchors / counterevidence / stopping_condition |
| `answer` | text |
| `tool` | entry / deps / usage |
| `curation` | target / added_anchors / count |
| `translation` | source / target / mapping |
| `question` | question / why_it_matters / context |

### ③ 复核别人的东西（**这是最重要的一步**）

```bash
python3 tools/sign review d-xxxxxx-0001 \
  --result pass \
  --reason "锚点可核查，反证条件具体" \
  --reproduce-minutes 45
```

**★ `--reproduce-minutes` 就是定价 —— 这是他在整套系统里最有分量的一个动作。**

它问的是：**别人从零做出同样的东西，要多久？**

- 不是"他花了多久"（那是交付者自己的事）
- 不是"这东西重不重要"
- 是**复现成本**

**为什么定价权在复核者手里而不是交付者手里**：交付者自报必然虚报且没法验证；**而复核者反正要读一遍，估这个是顺手的事，换个复核者还能交叉校对。**

**这堵住的是"会讲故事的人给自己加价"。**

**`--result fail` 也是正常结果**，而且被拒比"通过但没人用"好得多。

### ④ 提交与合并

```bash
python3 tools/sign check          # 提交前自查，不用等 CI
git checkout -b 我的分支
git add -A && git commit -m "deliver d-xxxxxx-0001"
git push -u origin 我的分支
```

然后在网页上开 PR。**接下来：**

```
CI 跑校验（签名 / 链 / 验收标准 / 复核者是否不同）
    ↓
另一个人复核（不能是他自己）
    ↓
开单的人确认
    ↓
维护者合并  ← 合并 = 入库 + 上账，一次完成
```

---

## 四、他会踩到的三个坑（以及怎么出来）

### 坑 1：`p-003：你不能复核自己交付的东西`

**原因**：他点的是自己交的东西。

**出来**：找另一个人来复核。**如果只有他一个人，那就先别交——等有人了再说。** 这不是限制，是设计：**自己闭环的东西不算贡献。**

### 坑 2：git 报冲突

**原因**：两个人同时从同一状态往下追加，**一定会**在 `data/requests.jsonl` 上冲突。

**出来**：

```bash
python3 tools/resolve --write
git add -A && git commit -m "merge: 解决冲突"
```

**它会把两边记录都保留、去重、重排、重链，而且不改内容——所以签名都还成立，不需要任何人的私钥。**

### 坑 3：`p-001 prev 对不上`

**原因**：链错位了（通常也是并发追加导致）。

**出来**：

```bash
python3 tools/fixlinks --write
```

---

## 五、"我凭什么信这个池子"

**三句话：**

1. **数据是五份纯文本文件**，一行一条 JSON，记事本就能打开
2. **任何人 clone 之后跑一条命令就能验证整份账**：

   ```bash
   python3 tools/validate
   ```

3. **它查三件事**：链是否完整（有没有被插入删改）、签名是否有效（是谁写的）、复核者是否与交付者不同（有没有自己批自己）

**不需要信任任何服务器，也不需要信任维护者。** 这就是全部的可信度来源：**一个哈希链加一个签名。**

**它防不住的**：防不了敷衍。签名能证明"某人说过"，证明不了"他真的看过"。**所以唯一能加分的项是"被别人引用"** —— 因为引用别人要先读懂它。

---

## 六、如果他想改规则

**能改。** 规则写在 [`policies.jsonl`](policies.jsonl) 里，就是一份文本文件。

改规则走的是**同一套流程**：开 issue → 提 PR → 校验 → 复核 → 合并。**只是需要两名维护者同意。**

**连"怎么改规则"这件事本身也在仓库里。** 所以这套东西不依赖任何外部权威——**只依赖"能不能被验证"。**

---

## 七、一张图

```
                        他打开网页
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
          看公告板        开一张单       看别人的交付
              │              │              │
              │              ▼              ▼
              │        提需求（issue）   复核（review）
              │              │           ★ 他在这里定价
              │              ▼              │
              │        有人接单、交付        │
              │              │              │
              └──────────────┴──────┬───────┘
                                    ▼
                              提交 → PR → CI 校验
                                    ▼
                          另一个人复核 → 开单人确认
                                    ▼
                            维护者合并 = 入库 + 上账
                                    ▼
                     展示页自动更新（Cloudflare Pages）
```
