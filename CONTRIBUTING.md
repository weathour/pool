# 五分钟上手

> **这份文档决定这个池子能不能活。** 校验器挡住的是坏人，而它决定好人会不会留下来。
> 目标：从零到开出第一张单，不超过 5 分钟。

---

## 这是什么

**一个公开的公告板加一本账。**

- **公告板**：谁需要什么，贴出来，别人看到了可以接
- **账本**：谁做了、值多少，由**三个人各签一句**记下来

它长在一个 git 仓库里。数据是四个纯文本文件，你 clone 下来就能看、就能验。

---

## 第 1 步：拿到仓库，装上密钥（2 分钟）

```bash
git clone <仓库地址> pool
cd pool

# 生成你自己的密钥，并登记到 keyring
python3 tools/sign init --handle 你的名字
```

它会告诉你私钥在哪、公钥是什么。

**★ 立刻做一件事：备份那个私钥文件。**

```bash
ls -l ~/.pool/keys/你的名字.key
```

拷到加密 U 盘或密码管理器。**丢了它，你所有记录的签名都无法再代表你**（记录不会消失，但你再也没法用那个身份签名了）。

**唯一的依赖：** Python 3.10+ 和 `cryptography`。

```bash
pip install cryptography    # 如果还没装
```

---

## 第 2 步：开一张单（1 分钟）

有两种开法，任选。

**方式 A：命令行**

```bash
python3 tools/sign req \
  --kind knowledge_pack \
  --title "需要一个关于 X 的辨析" \
  --spec "产物形态：一条带锚点的结论；范围：只覆盖 Y；精度：必须有反证条件" \
  --acceptance "包含至少 3 条可核查的锚点，且写明了什么证据会推翻它"
```

**方式 B：在网页上开 issue**（表单会强制你填同样几栏）

> **★ 两者是两件事，别搞混：**
>
> | | 是什么 | 谁看得到 |
> |---|---|---|
> | **网页 issue** | 公告 —— 「我想做这个」 | 协作界面上的人 |
> | **`data/requests.jsonl` 里的单** | **记录** —— 有签名、进账本、可验证 | 所有 clone 的人 |
>
> **网页 issue 不是记录，它不可验证。** 所以正式开单要用 `tools/sign req`；
> issue 只是让人先看到。**想让两边都有，就都做一次**（或者在有 issue 之后，
> 用 `tools/sign req --link <issue 编号>` 把记录挂上去）。

**★ `--acceptance` 是唯一不能省的。** 它回答的是"怎么算做完了"。

**写不出来就说明这张单还不合格**——因为整套记账的可信度全压在这一点上：**验收标准清楚，谁来判都一样；不清楚，就只能靠人品。**

**开单不需要任何人批准。** 它是你的一句声明，CI 过了就进主干。

---

## 第 3 步：接单、交付（1 分钟）

```bash
# 接别人开的单
python3 tools/sign claim r-0001

# 交东西（content 写在一个 JSON 文件里）
python3 tools/sign deliver \
  --request r-0001 \
  --kind knowledge_pack \
  --content-file 我的产物.json
```

`我的产物.json` 长什么样，看 `artifact_kind`：

| kind | 内容必填 |
|---|---|
| `knowledge_pack` | `claim` / `anchors` / `counterevidence` / `stopping_condition` |
| `answer` | `text` |
| `tool` | `entry` / `deps` / `usage` |
| `curation` | `target` / `added_anchors` / `count` |
| `translation` | `source` / `target` / `mapping` |
| `question` | `question` / `why_it_matters` / `context` |

---

## 第 4 步：提交，等复核（1 分钟）

```bash
# 提交前先自查，不用等 CI
python3 tools/sign check

git add -A
git commit -m "deliver d-0001: 关于 X 的辨析"
git push
```

然后**开一个 PR**。

**接下来会发生什么：**

1. CI 跑校验（签名、链、验收标准）
2. **另一个人来复核你的东西** —— 他不能是你
3. 复核通过、开单的人确认可用之后，**才入账**

**★ 关于 credit：** 值多少**不是你说了算，是复核的人估的**——他估"别人从零做出同样的东西要多久"。这是故意的：**把定价权从做事的人手里移到检查的人手里**，堵住"会讲故事的人给自己加价"。

---

## 如果 git 报冲突

两个人同时开单，一定会在 `data/requests.jsonl` 上冲突。**这是正常的。**

```bash
python3 tools/resolve --write    # 解决：去掉标记、两边都保留、重排、重链
git add -A && git commit -m "merge: 解决冲突"
```

**记录内容不会被改，所以签名都还成立，不需要任何人的私钥。**

---

## 你可能会问的四件事

**问：我能自己开单、自己接、自己交吗？**

能，形式合规。**但不会产生 credit**——因为需要他人复核。这是设计，不是限制：**自己闭环的东西不算贡献。**

**问：我被拒了怎么办？**

**被拒是正常结果，不是失败。** 而且驳回会留下记录——**记录不挂名，只在统计里**。

**"本期不产出任何东西"也是一个正式的成功决定。** 不要为了凑数硬交。

**问：我的私钥安全吗？**

私钥只在你自己机器上，**不在仓库里**。签名证明的是"这句话是你说的"，**不证明"你说得对"**。

**问：为什么不放区块链上？**

因为没必要，而且代价大。**git 的哈希链加签名已经给了全部需要的性质**，而数据上链会永久公开谁拿了多少。

**唯一值得借鉴的是时间戳**——用签名 tag 打一个就够。

---

## 规矩（就这几条）

**这些规矩不是装饰**：`policies.jsonl` 里的 11 条每一条都有校验器在跑，
而且每一条都有一个能抓住违规的反例测试。**CI 会拦住违规的提交。**

1. **只追加，不删除。** 记错了是再写一条"我改一下"，原记录永远在
2. **单子上不写人名。** 身份只在账本层，单子是干净的
3. **不产出就衰减。** 不是把人赶出去，是让位置自己空出来
4. **不能为了让检查通过而放松检查。** 卡住了只能改事实，不能改检查
5. **拉人进来不产生 credit。** 邀请不是产出

---

## 怎么验证这个池子

**三步，不需要读任何源码：**

```bash
# 1. 校验账本：链、签名、复核者是否不同、以及全部 11 条规则
python3 tools/validate

# 2. 验证周期 tag 的签名（证明"那一期就是这样"）
git config gpg.ssh.allowedSignersFile .allowed_signers
git tag -v cycle-2026-Q3

# 3. 看现状（--json 给机器读）
python3 tools/status
```

**这三步都不需要信任任何服务器，也不需要信任维护者。** 这就是全部的可信度来源。

---

## 出问题找谁

看 `MAINTAINERS.md`。

**维护者能推快，不能拖慢**——他不能单方面关你的单、也不能单方面改你的账。**有异议就提，他必须在 7 天内回应，哪怕回应是驳回。**
