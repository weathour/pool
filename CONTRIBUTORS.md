# 贡献者操作手册

> 给"要用这个池子的人"看。`CONTRIBUTING.md` 是五分钟速览，这份是完整手册。
> **如果你只想知道怎么开始，读 `CONTRIBUTING.md` 就够了。**

---

## 零、你需要什么

| 项 | 要求 |
|---|---|
| git | 任意近期版本（**你只是 clone 和 push，不需要 SHA-256 支持**） |
| Python | **3.10 或更高** |
| `cryptography` 包 | `pip install cryptography` |
| 一个私钥 | 用 `tools/sign init` 生成，**只存在你自己机器上** |

**就这样。** 不需要账号（除非要在网页上提 PR，那就需要一个 Gitea/GitHub 账号）。

---

## 一、第一次：装好身份（约 2 分钟）

```bash
git clone <仓库地址> pool
cd pool
python3 tools/sign init --handle 你的名字
```

输出会告诉你：

```
✓ 私钥：/home/你/.pool/keys/你的名字.key（权限 600）
  公钥：ed25519:xxxxx
  已登记到 data/keyring.jsonl，并记住 handle = 你的名字
```

**★ 第一件事：备份私钥。**

```bash
cp ~/.pool/keys/你的名字.key /media/U盘/pool-key-备份.key
```

**这个私钥就是你在这套系统里的全部身份。**

- 它 **不在仓库里**，我们也无法帮你找回
- 丢了之后：你**过去**的签名仍然有效（因为当时确实是你签的），但你**再也无法**用那个身份签新东西
- 补救办法：`tools/sign init --handle 新名字` 再登记一把，然后到 `keyring.jsonl` 里把旧的那条改成 `retired`（追加一条，不要删）

**别给私钥加密码。** 这不是疏忽：加了密码意味着你每次签名都要输一遍，那会让人不想签名。**代价由备份承担，不由密码承担。**

---

## 二、开一张单

**单子 = "我需要一个还不存在的产物"。**

### 命令行

```bash
python3 tools/sign req \
  --kind knowledge_pack \
  --title "一句话说清缺什么" \
  --spec "产物长什么样：形态、范围、精度要求" \
  --acceptance "怎么算做完了（必须可判定）"
```

| 参数 | 说明 |
|---|---|
| `--type` | `need`（我需要）· `offer`（我想做，缺人手）· `challenge`（已有产物是错的）。默认 `need` |
| `--kind` | 产物形态，见下表 |
| `--title` | 一句话 |
| `--spec` | 产物的形态、范围、精度 |
| `--acceptance` | **★ 怎么算做完了。唯一不能省的字段** |
| `--link` | 可重复，指向关联的板块 / 主题 / 已有产物 |

### 产物形态（`--kind`）

| kind | content 必填 | credit 初值 |
|---|---|---|
| `knowledge_pack` | `claim` / `anchors` / `counterevidence` / `stopping_condition` | 6 |
| `tool` | `entry` / `deps` / `usage` | 12 |
| `question` | `question` / `why_it_matters` / `context` | 见下 |
| `translation` | `source` / `target` / `mapping` | 3 |
| `curation` | `target` / `added_anchors` / `count` | 每锚点 0.5 |
| `answer` | `text` | 0.5 |

（credit 的最终值由**复核者**估的复现时间决定，上表只是默认参考。）

### ★ 关于 `--acceptance`

**这是整份手册里最重要的一段。**

一套记账能不能信，全压在"验收标准清不清楚"上：

- **清楚** → 谁来判都一样
- **不清楚** → 只能靠人品

**好的验收标准：**

> "包含至少 3 条可核查的锚点（格式 `path:行号`），且 `counterevidence` 栏写明了什么证据会推翻它"

**坏的验收标准：**

> "写得好一点" · "尽量全面" · "质量高"

**判断方法：把这句话给一个不认识你的人，他能不能只靠它决定"这东西算不算做完了"。** 能，就合格。

**写不出来，说明这张单还不该开。**

### 网页方式

在 Gitea/GitHub 上开 issue，表单会强制你填同样几栏。**开单不需要任何人批准**——CI 过了就进主干。

---

## 三、接单与交付

```bash
# 1. 接单
python3 tools/sign claim r-0001

# 2. 准备产物内容（一个 JSON 文件）
cat > 我的产物.json <<'EOF'
{
  "claim": "一句话结论",
  "anchors": ["corpus/xxx.md:42", "corpus/yyy.md:118"],
  "counterevidence": "如果出现 Z 这种材料，这个结论就不成立",
  "stopping_condition": "本结论只覆盖语料 A 到 B，不外推到 C"
}
EOF

# 3. 交付
python3 tools/sign deliver \
  --request r-0001 \
  --kind knowledge_pack \
  --content-file 我的产物.json
```

### 提交前自查

```bash
python3 tools/sign check
```

**这一步会跑完整校验，不用等 CI。** 建议养成习惯——推上去被 CI 打回来会多一轮。

---

## 四、提交与合并

```bash
git checkout -b my-delivery
git add -A
git commit -m "deliver d-0001: 关于 X 的辨析"
git push -u origin my-delivery
```

然后在网页上开 PR。

**接下来会发生什么：**

| 步骤 | 谁 | 说明 |
|---|---|---|
| CI 跑校验 | 自动 | 签名、链、验收标准、复核者是否不同 |
| 复核 | **另一个人**（不能是你） | 他读你的东西，并估"别人从零做要多久" |
| 确认 | 开单的人 | 确认产物确实能用来满足那张单 |
| 合并 | 维护者 | **合并 = 入库 + 上账，一次完成** |

---

## 五、复核别人的东西

```bash
python3 tools/sign review d-0007 \
  --result pass \
  --reason "锚点可核查，反证条件写得具体" \
  --reproduce-minutes 45
```

**★ `--reproduce-minutes` 是定价——这是你在这套系统里最重要的一个动作。**

它问的是：**别人从零做出同样的东西，要花多久？**

- 不是你花了多久（那是交付者自己的事）
- 不是"这东西重不重要"
- 是**复现成本**

**为什么定价权在你手里而不是在交付者手里：**

| 谁定价 | 后果 |
|---|---|
| 交付者自己报 | **必然虚报**，而且没法验证 |
| **复核者估** | 你反正要读一遍，估这个是顺手的事；**而且换个复核者可以交叉校对** |

**这堵住的是"会讲故事的人给自己加价"。**

### `--result fail` 也是正常结果

失败会留下记录，**但不挂名，只在统计里**。**被拒不是失败**——它比"通过但没人用"好得多。

**如果你觉得这东西形式合规但没价值，判 fail 并且写清理由。** 这是对池子最有用的一件事。

---

## 六、遇到问题

### 校验失败

```bash
python3 tools/validate
```

| 报错 | 意思 | 怎么办 |
|---|---|---|
| `p-001 prev 对不上` | 有人在你之前提交了，你的 `seq`/`prev` 过期了 | 见下面的"冲突" |
| `p-001 hash 与内容不符` | 记录被改过 | **不要改已落账的记录**，改为追加一条新的 |
| `p-002 签名无效` | 私钥不对，或记录被改过 | 确认 `POOL_HANDLE` 指向你自己 |
| `p-002 公钥未登记` | 你的 `keyring` 记录没在同一个 PR 里 | 把 `keyring.jsonl` 的改动一起提交 |
| `p-003 复核者与交付者相同` | 你在批自己 | 找另一个人 |

### credit 怎么真的发出来

**门槛是两件事都成立**：复核通过（复核者 ≠ 交付者）**且**开单人确认。

**入账由交付者本人签名**——因为 credit 是本人领的账。所以：

```bash
# confirm 时会自动入账；如果当时执行 confirm 的人本机没有交付者的私钥，
# 交付者自己补一条：
python3 tools/sign credit mint d-xxxxxx-0001
```

**数额规则：**

| 项 | 规则 |
|---|---|
| 定基 | 复核者估的复现时间 ÷ 10 |
| 复用加成 | ×(1 + min(引用次数, 5) × 0.2)，封顶 ×2.0 |
| 提问版税 | 引发的产物 credit 之和 × 20%，上限 200 |

**幂等**：同一件交付物不会重复入账。

### 都有哪些工具

| 工具 | 干什么 |
|---|---|
| `tools/sign` | 生成密钥、开单、接单、交付、复核、确认、修正 |
| `tools/validate` | 校验账本（CI 跑的就是它） |
| `tools/status` | 一眼看清现状 |
| `tools/sign credit mint` | 补入账（交付者本人执行） |
| `tools/sign reuse` | 记一次复用，并补发加成 |
| `tools/fixlinks` | 链尾错位时重算 `seq` / `prev` |
| `tools/resolve` | git 合并冲突后去掉标记、去重、重排、重链 |
| `tools/build-site` | 生成公开展示页 |
| `make check` | 跑全部测试 |

### 冲突：`prev` 对不上

**因为你和别人同时追加了，链条错位。** 解决：

**先说清一件事：两个人同时从同一个状态往下追加，git 一定会报冲突。**

因为追加发生在**同一位置**（文件末尾）。这是正常的，不是谁做错了。

**处理办法就一条命令：**

```bash
git fetch origin
git merge origin/main
#   → 冲突：data/requests.jsonl

python3 tools/resolve            # 先看，只报告
python3 tools/resolve --write    # 解决
git add -A && git commit -m "merge: 解决冲突"
```

**`resolve` 会做四件事：**

1. 去掉冲突标记（`<<<<<<<` `=======` `>>>>>>>`）
2. **两边的记录都保留**，按 `hash` 去重（共同祖先里的记录会在两侧各出现一次）
3. 按时间重排
4. 重算 `seq` 和 `prev`

**它不改记录内容——所以 `hash` 和签名都还成立，不需要任何人重新签名。**

**为什么可以这样：** 记录的签名只覆盖**内容**，不覆盖它在链上的**位置**。位置变了、内容没变，签名就该继续有效。这是 schema 里一个刻意的设计，就是为了让多人合并成为可能。

**如果 `resolve` 报出「需要人判断的重复」**（比如同一个 id 有两条 `open`），那说明**两个人真的开了同一张单**——这种情况脚本不猜，交给人。你决定留哪条、把另一条改成 `close` 或直接删掉（删掉是允许的，因为它还没被合并进主线）。

**还有一个更轻的工具：** 如果链只是错位（没有冲突标记），跑 `python3 tools/fixlinks --write` 就行。

### 私钥丢了

见第一节末尾。**过去的签名永远有效**，只是你没法再用那个身份签新的。

### 我想改一条已经落账的记录

**不能改，只能追加。**

```bash
python3 tools/sign amend <文件> <id> --reason "为什么改"
```

这会追加一条新记录，指向旧的那条 `hash`。**旧记录永远在。** 这不是防你，是防"事后悄悄改账"这件事本身。

---

## 七、规矩（就五条）

1. **只追加，不删除。** 记错了是再写一条"我改一下"，原记录永远在
2. **单子上不写人名。** 身份只在账本层，申请单是干净的
3. **不产出就衰减。** 不是把人赶出去，是让位置自己空出来
4. **不能为了让检查通过而放松检查。** 卡住了只能改事实，不能改检查
5. **拉人进来不产生 credit。** 邀请不是产出

---

## 八、这些规矩是谁定的、能不能改

**都能改。** 规则写在 `policies.jsonl` 里，就是一份文本文件。

改规则走的是**同一套流程**：

```bash
# 追加一条新规则，supersedes 指向被取代的那条
# 然后提 PR，需要两名维护者同意
```

**连"怎么改规则"这件事本身也在仓库里。** 所以这套东西不依赖任何外部权威——**只依赖"能不能被验证"。**

---

## 九、如果你想更深入

- **[`README.md`](README.md)** —— 这是什么
- **[`MAINTAINERS.md`](MAINTAINERS.md)** —— 谁管什么，维护者受什么约束
- **[`policies.jsonl`](policies.jsonl)** —— 十一条规则原文
- **`docs/`** —— 设计判断、被否掉的方案、踩过的坑

**最后一条最值得看：** 里面记着**为什么很多东西被否掉了**（不用区块链、不做分层公开、不设元老院、不做数据库）。**知道什么被排除掉，比知道选了什么更重要。**
