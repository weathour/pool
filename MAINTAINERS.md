# 维护者

> 这份文件说清：谁管哪个板块、能做什么、不能做什么。
> **它是权威的**——`CODEOWNERS` 是从这份文件生成的，换平台时重新生成即可。
> 本文件自身的每一次改动都必须在一个周期内公示（见 `policies.jsonl` 的 p-010）。

## 约束：能推快，不能拖慢

| 维护者**可以**单方面做 | 维护者**不能**单独做 |
|---|---|
| 确认交付物、推进状态 | **关单**（含"决定不做"） |
| 回应急议（哪怕回应是驳回） | **推翻已通过的复核** |
| 合并 `requests` / `deliverables` 的常规提交 | **修改 credit 认定**（`credits.jsonl`） |
| | **修改本文件或 `policies.jsonl`** |

右边三件事**必须第二人同意**。

**为什么这样设计：** 一个人掌权时最常用、最难防的手段不是乱做事，是**不回应**。所以异议必须在 7 天内回应——不回应就自动升级。

## 名单

| 板块 | 维护者 | 公钥 | 自 |
|---|---|---|---|
| **全部（创始）** | 贾苇索 (JiaWeathour) | 见 `data/keyring.jsonl` 的 `k-0001` | 2026-09-16 |

## 有待认领的板块

**目前只有一个维护者。** 任何板块都可以拆出去给新维护者——这是扩展的正常路径，也是防止单点的唯一办法。

板块候选（按热度，随时可加）：

| 板块 | 管什么 |
|---|---|
| `corpus/` | 语料接入、原始材料 |
| `library/` | 结论、卡片、主题档案 |
| `experience/` | 踩过的坑、当时的判断理由 |
| `tools/` | 校验器、签名工具、站点生成 |
| `policies.jsonl` | 规则本身（★ 这一块必须两名维护者共同持有） |

## 最后一个维护者离开了会怎样

**仓库和记录不因此失效**——任何人 clone 之后跑 `python3 tools/validate` 都能验证。

但：

- 任何持钥者仍可提 PR，**但无人能合并**
- 规则：**只有现任维护者能添加新维护者**
- 所以维护者消失 = **该板块冻结，不是腐烂**

**冻结是可接受的，腐烂不是。** 这是这套设计明确选择的状态。

## 缺席接管

维护者连续 **30 天**未响应，由其他维护者中最先响应者接管该板块，并在本文件公示。
（只有一名维护者时，本条不适用——见上一节。）

## 退出

维护者可随时退出。退出时**必须**完成三件事：

1. **账本交接**：确认 `data/` 已在至少两个独立位置存在（本机 + 至少一个远端/镜像）
2. **交接演练**：让接手者实际做一次合并操作（没演练过的交接流程等于没有）
3. **公示**：在本文件写明变更与理由
